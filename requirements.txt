# Core: data profiling, splitting, eval harness, popularity/CF/MF tiers (Week 1-2)
pandas>=2.2
pyarrow>=15.0
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4
implicit>=0.7
tqdm>=4.66
matplotlib>=3.8

# Notebooks: EDA/profiling only (pipeline steps -- split, eval, training --
# stay as .py so they're importable and diff cleanly; see build-log.md)
jupyter>=1.0
nbconvert>=7.16
ipykernel>=6.29

# Kaggle CLI (dataset download) — already available globally, listed for reproducibility
kaggle>=1.6
